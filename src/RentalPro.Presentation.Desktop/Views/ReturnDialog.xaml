<Window x:Class="RentalPro.Presentation.Desktop.Views.ReturnDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Оформить возврат"
        Width="720"
        Height="760"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FB">

    <Window.Resources>
        <Style x:Key="PrimaryDialogButtonStyle"
               TargetType="Button">
            <Setter Property="Background" Value="#1E73FF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="22 0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Border Margin="20"
            Padding="22"
            Background="White"
            CornerRadius="16">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Верх -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Оформить возврат"
                               FontSize="22"
                               FontWeight="SemiBold"
                               Foreground="#071B39" />

                    <TextBlock Text="{Binding OrderNumber, StringFormat=Заказ № {0}}"
                               FontSize="13"
                               Foreground="#6B7890"
                               Margin="0 6 0 0" />
                </StackPanel>

                <Button Grid.Column="1"
                        Click="Cancel_OnClick"
                        Style="{StaticResource MaterialDesignIconButton}">
                    <materialDesign:PackIcon Kind="Close"
                                             Width="22"
                                             Height="22"
                                             Foreground="#64748B" />
                </Button>
            </Grid>

            <!-- Клиент -->
            <Border Grid.Row="2"
                    Background="#F8FAFC"
                    BorderBrush="#D8E2F0"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="16">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Клиент"
                               FontSize="15"
                               Foreground="#6B7890" />

                    <TextBlock Grid.Column="1"
                               Text="{Binding CustomerName}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#071B39" />
                </Grid>
            </Border>

            <!-- Список инструментов -->
            <Border Grid.Row="4"
                    BorderBrush="#E2E8F0"
                    BorderThickness="1"
                    CornerRadius="16"
                    Background="White">

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              MaxHeight="330">

                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="14"
                                        BorderBrush="#EEF2F7"
                                        BorderThickness="0 0 0 1">

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center" />

                                        <StackPanel Grid.Column="1"
                                                    Margin="12 0 0 0">
                                            <TextBlock Text="{Binding ToolName}"
                                                       FontWeight="SemiBold"
                                                       Foreground="#071B39" />

                                            <TextBlock Text="{Binding PlannedReturnDate, StringFormat=Плановый возврат: {0:dd.MM.yyyy}}"
                                                       FontSize="12"
                                                       Foreground="#6B7890"
                                                       Margin="0 6 0 0" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </ScrollViewer>
            </Border>

            <!-- Дата / состояние -->
            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <DatePicker Grid.Column="0"
                            Height="56"
                            SelectedDate="{Binding ActualReturnedDate, UpdateSourceTrigger=PropertyChanged}"
                            materialDesign:HintAssist.Hint="Дата возврата"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}" />

                <ComboBox Grid.Column="2"
                          Height="56"
                          ItemsSource="{Binding Conditions}"
                          SelectedItem="{Binding ReturnCondition, UpdateSourceTrigger=PropertyChanged}"
                          materialDesign:HintAssist.Hint="Состояние"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}" />
            </Grid>

            <!-- Комментарий -->
            <TextBox Grid.Row="8"
                     Height="70"
                     Text="{Binding DamageComment, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalContentAlignment="Top"
                     materialDesign:HintAssist.Hint="Комментарий о повреждениях"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}" />

            <!-- Низ -->
            <Grid Grid.Row="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#DC2626"
                           FontSize="13"
                           VerticalAlignment="Center" />

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">

                    <Button Height="42"
                            Padding="18 0"
                            Click="Cancel_OnClick"
                            Style="{StaticResource MaterialDesignOutlinedButton}">
                        <TextBlock Text="Отмена" />
                    </Button>

                    <Button Margin="12 0 0 0"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource PrimaryDialogButtonStyle}">

                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CheckCircleOutline"
                                                     Width="18"
                                                     Height="18"
                                                     Margin="0 0 8 0"
                                                     Foreground="White" />

                            <TextBlock Text="Оформить возврат"
                                       Foreground="White"
                                       FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>